# Easy task queue example

Run `docker-compose up`

open `localhost:80`

click "Add task" as many times as you want

Workers will wait for some seconds and the nsend a random number.

Badabing badabum
